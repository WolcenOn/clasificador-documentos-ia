PORT=8080
APP_TOKEN=cambia_este_token

# Gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash
# off | always | low_confidence | missing_only
GEMINI_MODE=off

# PostgreSQL, se usará en la siguiente fase
DATABASE_URL=
